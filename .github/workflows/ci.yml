name: build

on:
  push:
    branches:
    tags:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: set up go 1.25
        uses: actions/setup-go@v6
        with:
          go-version: "1.25"
        id: go

      - name: build and test
        run: |
          go test -timeout=3m -covermode=count -coverprofile=$GITHUB_WORKSPACE/profile.cov_tmp ./...
          cat $GITHUB_WORKSPACE/profile.cov_tmp | grep -v -E "mocks|/enum/" > $GITHUB_WORKSPACE/profile.cov
        env:
          TZ: "America/Chicago"

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest

      - name: install goveralls
        run: go install github.com/mattn/goveralls@latest

      - name: submit coverage
        run: |
          goveralls -service="github" -coverprofile=$GITHUB_WORKSPACE/profile.cov
        env:
          COVERALLS_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  python-sdk:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: install dependencies
        working-directory: lib/stash-python
        run: uv sync --all-extras

      - name: run tests
        working-directory: lib/stash-python
        run: uv run pytest --cov --cov-report=term-missing

      - name: run linter
        working-directory: lib/stash-python
        run: uv run ruff check .

  typescript-sdk:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: lib/stash-js/package-lock.json

      - name: install dependencies
        working-directory: lib/stash-js
        run: npm ci

      - name: run type checking
        working-directory: lib/stash-js
        run: npm run typecheck

      - name: run tests
        working-directory: lib/stash-js
        run: npm test

      - name: run linter
        working-directory: lib/stash-js
        run: npm run lint

      - name: build
        working-directory: lib/stash-js
        run: npm run build

  java-sdk:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: setup java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: setup gradle
        uses: gradle/actions/setup-gradle@v4

      - name: run tests
        working-directory: lib/stash-java
        run: ./gradlew test

      - name: build
        working-directory: lib/stash-java
        run: ./gradlew build
