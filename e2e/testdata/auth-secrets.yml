# e2e test auth configuration for secrets tests
# all users have password: testpass

users:
  # admin with full secrets access (all secrets paths)
  - name: admin
    password: $2a$10$NgkQ//qToD01pjDUq12/neYkmOoVwQteZ8g.M.j4lUSeN/K9RQzU2
    permissions:
      - prefix: "*"
        access: rw
      - prefix: "secrets/*"
        access: rw
      - prefix: "app/secrets/*"
        access: rw

  # user without secrets access (wildcard only)
  - name: noSecrets
    password: $2a$10$NgkQ//qToD01pjDUq12/neYkmOoVwQteZ8g.M.j4lUSeN/K9RQzU2
    permissions:
      - prefix: "*"
        access: rw

  # user with scoped secrets access (app/secrets/* only)
  - name: appSecrets
    password: $2a$10$NgkQ//qToD01pjDUq12/neYkmOoVwQteZ8g.M.j4lUSeN/K9RQzU2
    permissions:
      - prefix: "app/*"
        access: rw
      - prefix: "app/secrets/*"
        access: rw
